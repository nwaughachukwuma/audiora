<script lang="ts">
	// import { browser } from '$app/environment';
	// import { goto } from '$app/navigation';
	// import { page } from '$app/stores';
	// import { sessionStore } from '$lib/stores/session';
	// import ChatInterface from '$lib/components/ChatInterface.svelte';
	// import AudioInterface from '$lib/components/AudioInterface.svelte';

	// $: userSpecification = $sessionStore.userSpecification;
</script>

<svelte:head>
	<title>Audiora</title>
</svelte:head>

<div class="max-w-4xl mx-auto">
	<h1 class="text-4xl font-bold mb-2">🎧 Audiora</h1>
	<h2 class="text-2xl mb-8">Listen to anything, anytime, leveraging AI</h2>

	<!-- {#if !userSpecification}
	  <ChatInterface />
	{:else}
	  <AudioInterface />
	{/if} -->
</div>
